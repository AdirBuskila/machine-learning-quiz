# ASK ADIR — what's left (most of the original list is now DONE)

**Status:** the bank is now **322 questions**, all answer-verified, including **~40 figure
questions** cropped from the PDFs and connected, plus the **2025 סמסטר ב׳** exam recovered
from the image-only `.docx` (19 questions, answers read from the green ✓). All figures were
reviewed on a contact sheet; broken/clipped crops were fixed or removed.

## ✅ Done since the overnight build
- Cropped & connected ~40 figures (confusion matrices, decision-tree curves, scatter plots,
  K-means/circle diagrams, neural-net diagram, chessboards, truth tables, vector-arrow
  image-options). 52 image files in `images/exams/`, all verified to exist and render.
- Added the 2025 semester-B exam (image-only .docx) via vision transcription.

## Still worth your input (small list now)

### 1. Figure questions in the 4 UN-keyed exams (still excluded)
These files had no reliable answer key (all-bold font / scanned / no solutions), so their
figure questions are still out — I won't key them by guessing:
- `22A-A` (2022 סמ׳ ב מועד ב, figure-heavy), `22A-C` (2022 סמ׳ א מועד ג, all-bold),
  `22B-B` (2022 סמ׳ ב מועד ב פתרונות, all-bold), `24B-B` (2024 סמ׳ ב מועד ב, no key).
- To include any: I need the solutions/answer key for that sitting, then I can crop + add.

### 2. One dropped question
- `24B-C Q19` (KNN, needs a data table that cropped badly). Answer is known (ג). If you want
  it, say so and I'll re-crop the table properly.

### 3. A few "plain" figure crops (functional, but you may want to eyeball)
These are formula/vector-as-image crops — they contain the needed values but look plain
rather than pretty: `24B-A-Q3`, `24B-C-Q14`, `24S-A-Q11/12/14`, `SAMP-2-Q15`. They work;
tell me if you'd rather I inline the values as text instead.

### 4. Sources still not mined (optional, for more volume)
- **2022 סמסטר א׳ מועד ב׳** — scanned images → needs OCR.
- **2023 מועד ג׳** `.docx` — near-empty read.
- **2024 קיץ מועד ב׳** and any 2024 sitting with no key — need solutions to key safely.
- **Big aggregates**: `מבחנים דניאל.pdf` (45 MB), `תרגילים(2022).pdf` (69pg) — I can mine
  these for a lot more questions if you want.

## Fastest wins
1. Skim the app (now with figures) and flag anything that reads wrong.
2. Want the 4 un-keyed exams' figures in? Send me their solutions files.
3. Want more volume? Say the word and I'll OCR the 2022 scans + mine the big `דניאל` file.
